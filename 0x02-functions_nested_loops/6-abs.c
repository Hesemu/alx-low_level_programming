#include "main.h"
/**
*_abs - function computes the absolute value of an integer
*@i: integer whose absolute value is to be computed
*Return: Always i for success and -i otherwise
*/
int _abs(int i)
{
	int i;

	if (i >= 0)
	{
		return (i);

	}
	else
		return (-i);

}
